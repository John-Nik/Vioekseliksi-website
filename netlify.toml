[build]
	functions = "../netlify/functions"
	publish = "public"

[functions]
    node_bundler = "esbuild"
	directory = "/netlify/functions"

[dev]
	command = "npm run server"
	targetPort = 8080
	port = 3001
	publish = "public"
	framework = "eleventy"